name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: cmake
      run: mkdir -p c++/build && cd c++/build && cmake ..
    - name: make
      run: make
    - name: make check
      run: make check
